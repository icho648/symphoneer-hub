{
  "name": "@symphoneer-hub/smoke-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/counter.test.js"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
